# Git 练习项目 🚀

这是一个用于练习 Git 操作的示例项目，包含一个简单的网页应用。

## 📁 项目结构

```
sourcetree/
├── README.md          # 项目说明文件（你正在看的这个文件）
├── index.html         # 首页HTML文件
├── styles.css         # 样式文件
├── script.js          # JavaScript交互代码
└── .gitignore         # Git忽略规则
```

## 🎯 学习目标

通过这个项目，您将练习以下 Git 操作：

- ✅ **基础提交** (commit)
- ✅ **推送/拉取** (push/pull)
- ✅ **分支管理** (branch)
- ✅ **合并操作** (merge)
- ✅ **冲突解决** (conflict resolution)
- ✅ **查看历史** (log)
- ✅ **回退操作** (reset/revert)

## 🚀 快速开始

### 1. 初始化 Git 仓库

```bash
git init
git add .
git commit -m "Initial commit: 创建项目基础文件"
```

### 2. 查看项目

在浏览器中打开 `index.html` 文件，查看网页效果。

## 📚 Git 操作练习步骤

### 基础操作

#### 步骤 1: 查看状态

```bash
git status
```

#### 步骤 2: 修改文件并提交

```bash
# 修改 index.html 中的内容（比如更改标题）
git add index.html
git commit -m "修改页面标题"
```

#### 步骤 3: 查看提交历史

```bash
git log
git log --oneline  # 简洁模式
git log --graph    # 图形化显示
```

### 分支操作

#### 步骤 4: 创建并切换分支

```bash
git branch feature-style     # 创建新分支
git checkout feature-style   # 切换到新分支
# 或者使用一条命令：
git checkout -b feature-style
```

#### 步骤 5: 在新分支上开发

```bash
# 修改 styles.css 文件（比如更改颜色）
git add styles.css
git commit -m "更新页面配色方案"
```

#### 步骤 6: 查看所有分支

```bash
git branch        # 查看本地分支
git branch -a     # 查看所有分支（包括远程）
```

### 合并操作

#### 步骤 7: 合并分支

```bash
git checkout main              # 切换回主分支
git merge feature-style        # 合并feature-style分支
```

### 练习冲突解决

#### 步骤 8: 制造冲突并解决

```bash
# 1. 在main分支修改script.js的某一行
git checkout main
# 修改 script.js 文件
git add script.js
git commit -m "在main分支修改计数器功能"

# 2. 切换到新分支，修改同一行
git checkout -b feature-counter
# 修改 script.js 的同一位置
git add script.js
git commit -m "在feature分支修改计数器功能"

# 3. 尝试合并，会产生冲突
git checkout main
git merge feature-counter

# 4. 手动解决冲突
# 打开script.js，找到冲突标记：
# <<<<<<< HEAD
# =======
# >>>>>>> feature-counter
# 手动编辑文件，保留需要的内容

# 5. 提交解决后的文件
git add script.js
git commit -m "解决合并冲突"
```

## 💡 建议的练习任务

1. **任务 1：修改样式**

   - 创建分支 `feature-new-colors`
   - 修改 `styles.css` 中的颜色
   - 提交并合并到主分支

2. **任务 2：添加新功能**

   - 创建分支 `feature-timer`
   - 在 `script.js` 中添加定时器功能
   - 在 `index.html` 中添加对应的 UI
   - 提交并合并

3. **任务 3：练习 rebase**

   - 创建分支 `feature-refactor`
   - 进行多次提交
   - 使用 `git rebase -i` 整理提交历史

4. **任务 4：练习撤销**
   - 使用 `git reset` 撤销提交
   - 使用 `git revert` 回退某个提交
   - 使用 `git checkout -- <file>` 撤销文件修改

## 🔧 常用 Git 命令速查

### 基础命令

```bash
git init                      # 初始化仓库
git status                    # 查看状态
git add <file>               # 添加文件到暂存区
git add .                    # 添加所有文件
git commit -m "message"      # 提交
git log                      # 查看历史
```

### 分支命令

```bash
git branch                   # 查看分支
git branch <name>           # 创建分支
git checkout <name>         # 切换分支
git checkout -b <name>      # 创建并切换分支
git merge <name>            # 合并分支
git branch -d <name>        # 删除分支
```

### 远程操作

```bash
git remote add origin <url>  # 添加远程仓库
git push -u origin main      # 推送到远程
git pull                     # 拉取更新
git clone <url>             # 克隆仓库
```

### 高级命令

```bash
git reset --hard <commit>    # 重置到某个提交
git revert <commit>          # 回退某个提交
git stash                    # 暂存当前修改
git stash pop                # 恢复暂存的修改
git rebase <branch>          # 变基
```

## 📝 提示

- 这是一个纯练习项目，可以随意实验！
- 建议每次只做一个练习，这样更容易理解每个命令的作用
- 如果搞砸了，可以删除 `.git` 文件夹重新开始
- 使用 `git log --graph --oneline --all` 可以看到漂亮的分支图

## 🎉 下一步

当你熟悉了基本操作后，可以：

1. 在 GitHub/GitLab 上创建远程仓库
2. 练习推送和拉取
3. 尝试多人协作场景
4. 学习 Git 工作流（Git Flow, GitHub Flow 等）

祝你学习愉快！💪
